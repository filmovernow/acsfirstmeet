#include <stdio.h>
int main(){
    int num1,num2,num3,sum,max,matrix[9],i;
    float average;
    for (i=0;i<9;i++)
    {
        scanf("%d",&matrix[i]);
    
    }
        max=matrix[0];
        for(i=1;i<9;i++)
        if (matrix[2]>matrix[1]);
}
